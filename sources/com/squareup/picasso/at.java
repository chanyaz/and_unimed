package com.squareup.picasso;

import java.util.concurrent.ThreadFactory;

class at implements ThreadFactory {
    at() {
    }

    public Thread newThread(Runnable runnable) {
        return new as(runnable);
    }
}
