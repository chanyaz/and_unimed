package okhttp3;

import javax.annotation.Nullable;

public interface Authenticator {
    public static final Authenticator a = new Authenticator() {
        public ad authenticate(ak akVar, ag agVar) {
            return null;
        }
    };

    @Nullable
    ad authenticate(ak akVar, ag agVar);
}
